<?php

use Illuminate\Support\Facades\Route;

// sesuaikan dulu kontroller nya
use App\Http\Controllers\{{ controller }};

Route::group(['prefix' => '{{ class }}',  'middleware' => ['web']], function()
{
    Route::get('/', [{{ controller }}::class, 'index']);
});
